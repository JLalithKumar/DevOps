apiVersion: v1
kind: Pod
metadata:
  name: good-pod
  namespace: irsa-demo

spec:
  serviceAccountName: customer-sa

  containers:
  - name: aws-cli
    image: amazon/aws-cli
    command:
      - sleep
      - "3600"